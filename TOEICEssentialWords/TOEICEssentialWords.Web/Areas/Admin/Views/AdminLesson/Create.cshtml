@using TOEICEssentialWords.Web.Areas.Admin.ViewModels
@model AdminLessonViewModel

<div class="modal-dialog" role="document">
    <div class="modal-content">
        @using (Ajax.BeginForm("Create", "AdminLesson", FormMethod.Post, new AjaxOptions
        {
            InsertionMode = InsertionMode.Replace,
            HttpMethod = "POST",
            UpdateTargetId = "lessonModal",
            OnSuccess = "AdminLesson.Success(data)"
        }))
        {
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <h4 class="modal-title" id="lessonModalLabel">Create New Lesson</h4>
            </div>
            <div class="modal-body">
                @Html.ValidationSummary()
                @Html.AntiForgeryToken()
                <div class="form-group">
                    @Html.LabelFor(model => model.LessonNumber, new { @class = "control-label" })
                    @Html.TextBoxFor(model => model.LessonNumber, new { @class = "form-control" })
                    @Html.ValidationMessageFor(model => model.LessonNumber)
                </div>
                <div class="form-group">
                    @Html.LabelFor(model => model.Name, new { @class = "control-label" })
                    @Html.TextBoxFor(model => model.Name, new { @class = "form-control" })
                    @Html.ValidationMessageFor(model => model.Name)
                </div>
                <div class="form-group">
                    @Html.LabelFor(model => model.TopicId, new { @class = "control-label" })
                    @Html.DropDownListFor(model => model.TopicId, Model.AllTopics, new { @class = "form-control" })
                    @Html.ValidationMessageFor(model => model.TopicId)
                </div>
            </div>
            <div class="modal-footer">
                <button type="submit" class="btn btn-success">Create</button>
                <button type="button" class="btn btn-danger" data-dismiss="modal">Cancel</button>
            </div>
        }
    </div>
</div>